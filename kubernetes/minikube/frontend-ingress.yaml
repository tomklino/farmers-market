apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: frontend
spec:
  rules:
  - host: farmers.local
    http:
      paths:
      - path: /
        backend:
          serviceName: frontend
          servicePort: 80

