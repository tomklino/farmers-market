---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: image-server
spec:
  rules:
  - host: farmers.local
    http:
      paths:
      - path: /images
        backend:
          serviceName: image-server
          servicePort: 80
